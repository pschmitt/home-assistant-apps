ignore:
  - DL3006  # Always tag FROM image explicitly — intentional, HA app base images use rolling tags
  - DL3007  # Using latest tag is prone to errors — intentional, we want latest base images
  - DL3008  # Pin versions in apt-get install — intentional, unpinned picks up security patches
  - DL3013  # Pin versions in pip install — intentional, unpinned picks up security patches
  - DL3018  # Pin versions in apk add — intentional, unpinned picks up security patches
